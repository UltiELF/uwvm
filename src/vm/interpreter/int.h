#pragma once
#include "codegen.h"
#include "codestorge.h"
#include "../../run/wasm_file.h"

namespace uwvm::interpreter
{
    inline void interpret() noexcept 
    { 
        auto& wasmmod{::uwvm::global_wasm_module};
        if(wasmmod.startsec.sec_begin) 
        {

        }
        else if(wasmmod.startsec.sec_begin) 
        {
        }
    }

}
